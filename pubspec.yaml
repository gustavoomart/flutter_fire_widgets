name: fire_widgets
description: "Fire widgets is a collection of various widgets to make creating aplicatpions easier."
version: 1.0.2
repository: https://github.com/gustavoomart/flutter_fire_widgets


environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0